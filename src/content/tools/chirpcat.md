---
name: ChirpCat
vendor: Electronic Cats
type: software
ec: true
protocols:
  - LoRa
repo: 'https://github.com/ElectronicCats/chirpcat'
note: >-
  LoRa/LoRaWAN discovery, classification and capture platform. Listens to
  LoRaWAN traffic — both uplink and downlink frames — through a gateway backend
  (Semtech UDP packet forwarder on port 1700), clusters packets by RF
  characteristics, scores/classifies targets and dispatches follow/capture tasks
  to CatSniffer workers. Designed to run on the RAK WisGate Connect (Raspberry
  Pi CM4).
---
LoRa/LoRaWAN discovery, classification and capture platform. Listens to LoRaWAN traffic — both uplink and downlink frames — through a gateway backend (Semtech UDP packet forwarder on port 1700), clusters packets by RF characteristics, scores/classifies targets and dispatches follow/capture tasks to CatSniffer workers. Designed to run on the RAK WisGate Connect (Raspberry Pi CM4).
