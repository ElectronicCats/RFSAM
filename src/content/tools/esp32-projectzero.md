---
slug: esp32-projectzero
name: projectZero
vendor: LOCOSP
type: project
protocols:
  - Wi-Fi
repo: 'https://github.com/LOCOSP/projectZero'
note: >-
  A two-board ESP32 evil-twin / deauther whose captive portal VERIFIES the
  entered password against the real AP, plus a WPA3-SAE overflow mode (MIT,
  active but small/early). Runs on ESP32 and ESP32-C5, driven from a CLI or a
  Flipper Zero. Low-maturity project — verify its behaviour on the bench before
  field use; the password-verifying portal and SAE-overflow angle are what make
  it worth noting.
---
A two-board ESP32 evil-twin / deauther whose captive portal VERIFIES the entered password against the real AP, plus a WPA3-SAE overflow mode (MIT, active but small/early). Runs on ESP32 and ESP32-C5, driven from a CLI or a Flipper Zero. Low-maturity project — verify its behaviour on the bench before field use; the password-verifying portal and SAE-overflow angle are what make it worth noting.
