---
slug: esp32-sour-apple
name: ESP32 Sour Apple
vendor: RapierXbox
type: project
protocols:
  - BLE
repo: 'https://github.com/RapierXbox/ESP32-Sour-Apple'
note: >-
  A focused PoC that crashes/freezes iOS devices by flooding them with BLE
  pairing-request advertisements (~595 stars, GPL-3.0). Tested on ESP32-S3 and
  ESP-WROOM-32 (an ESP8266 cannot run it — no BLE radio). Genuinely disruptive —
  it can freeze nearby iPhones — so authorised testing only, on hardware you
  own. Target/payload effectiveness dates as Apple patches.
---
A focused PoC that crashes/freezes iOS devices by flooding them with BLE pairing-request advertisements (~595 stars, GPL-3.0). Tested on ESP32-S3 and ESP-WROOM-32 (an ESP8266 cannot run it — no BLE radio). Genuinely disruptive — it can freeze nearby iPhones — so authorised testing only, on hardware you own. Target/payload effectiveness dates as Apple patches.
