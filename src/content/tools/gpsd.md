---
name: gpsd
vendor: gpsd project (Eric S. Raymond et al.)
type: software
protocols:
  - GNSS
repo: 'https://gitlab.com/gpsd/gpsd'
homepage: 'https://gpsd.io/'
note: >-
  The GPS service daemon: it reads the NMEA 0183 / UBX stream from a GNSS
  receiver on a serial or USB port, parses it, and re-serves the solved
  position/velocity/time to clients over a simple TCP/JSON protocol — with
  gpsmon and cgps for live terminal display of the fix, satellites and raw
  sentences. The standard way to consume a real GPS receiver on Linux/BSD and
  the normal 'see what the receiver reports' tool. Hosted on GitLab/Savannah,
  not GitHub.
---
The GPS service daemon: it reads the NMEA 0183 / UBX stream from a GNSS receiver on a serial or USB port, parses it, and re-serves the solved position/velocity/time to clients over a simple TCP/JSON protocol — with gpsmon and cgps for live terminal display of the fix, satellites and raw sentences. The standard way to consume a real GPS receiver on Linux/BSD and the normal 'see what the receiver reports' tool. Hosted on GitLab/Savannah, not GitHub.
