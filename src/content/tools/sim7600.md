---
name: SIMCom SIM7600
vendor: SIMCom
type: hardware
protocols:
  - LTE
spec: LTE Cat-4 modem · multi-band sub-6 GHz
homepage: 'https://www.simcom.com'
note: >-
  Low-cost commercial SIMCom LTE Cat-4 modem (Qualcomm MDM-based). Reports
  serving- and neighbour-cell information through its own AT command set —
  AT+CPSI? returns the system mode, operator (MCC-MNC), LAC, Cell ID, band and
  EARFCN — a no-SDR way to read which cell a target is on and identify it.
  Because the baseband is Qualcomm and exposes a /dev/diag port, the same module
  can also feed QCSuper to capture raw cellular signalling.
---
Low-cost commercial SIMCom LTE Cat-4 modem (Qualcomm MDM-based). Reports serving- and neighbour-cell information through its own AT command set — AT+CPSI? returns the system mode, operator (MCC-MNC), LAC, Cell ID, band and EARFCN — a no-SDR way to read which cell a target is on and identify it. Because the baseband is Qualcomm and exposes a /dev/diag port, the same module can also feed QCSuper to capture raw cellular signalling.
