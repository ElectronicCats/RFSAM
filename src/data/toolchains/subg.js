export default {
  "status": "complete",
  "facts": [
    { "k": "Bands", "v": "Unlicensed sub-GHz ISM, regional: 315 MHz (North America/Asia remotes & TPMS) · 433.92 MHz (the global workhorse — remotes, sensors, alarms) · 868 MHz (EU — wM-Bus meters, smart-home) · 915 MHz (US ISM, 902–928)" },
    { "k": "Modulation", "v": "Almost everything is OOK/ASK (on-off keying — the carrier blinks the bits, cheapest to build) or (G)FSK (the carrier shifts between two frequencies). A handful use minor variants; there is no spread spectrum here, so the signals are simple to demodulate once you know the baud rate." },
    { "k": "Encoding / baud", "v": "Bits are usually PWM/Manchester/PPM at a few hundred to a few thousand baud, in short repeated bursts. A frame is typically an ID + a few state/sensor bytes + a checksum — small, and often repeated several times per press for reliability." },
    { "k": "Code type", "v": "Fixed-code (the same payload every time — trivially captured and replayed) vs rolling-code / hopping (each press sends a new value from a counter, e.g. KeeLoq, HCS301): a naive replay of an old rolling code is rejected by the receiver." },
    { "k": "Crypto", "v": "Most sub-GHz devices have NO confidentiality — fixed-code remotes and sensors send their payload in the clear. Rolling codes add replay resistance, not encryption you decrypt from a capture; KeeLoq's block cipher needs the manufacturer key to forge a valid next code, which a passive capture does not yield." },
    { "k": "Targets", "v": "Garage/gate remotes & car key fobs, TPMS sensors, wireless weather/soil/temperature stations, 433 MHz smart-home plugs and doorbells, wM-Bus smart meters, and alarm door/window contacts." }
  ],
  "reference": null,
  "layers": {
    "IG": {
      "note": "Work out what the device is before any RF work — no capture yet. Sub-GHz is a grab-bag of cheap one-way gadgets, so the first job is to pin down four things: which frequency it transmits on, whether it is OOK/ASK or FSK, roughly how fast it sends, and — the question that decides everything downstream — whether it is a fixed code (the same payload every time, replayable) or a rolling/hopping code (a new value each press, e.g. KeeLoq). Much of this comes off the device itself: the FCC ID printed on the label looks up the exact frequency, modulation and a test report on fccid.io, and a teardown reveals the radio chip (a CC1101, a PT2262/EV1527 OOK encoder, an HCS301/KeeLoq rolling-code chip…).",
      "lookFor": [
        "Carrier frequency — 315 / 433.92 / 868 / 915 MHz. The FCC ID on the label resolves the exact frequency and band (fccid.io); a quick gqrx sweep of the ISM bands confirms it. This sets every radio and tool choice below.",
        "Modulation — OOK/ASK (the carrier blinks on and off — by far the most common) vs (G)FSK (the carrier shifts between two tones). Visible on a waterfall and reported by rtl_433/URH; it sets the demodulator settings.",
        "Encoding and baud rate — PWM / Manchester / PPM at a few hundred to a few thousand baud, sent as short repeated bursts. URH auto-estimates it; you need it to turn the burst into bits.",
        "Fixed vs rolling code — does the same press always send the same payload (fixed: PT2262/EV1527-class, replayable) or a different value each time (rolling/hopping: KeeLoq/HCS301)? This single fact decides whether a plain capture-and-replay works or you need a RollJam-style attack.",
        "Radio chip and encoder (FCC ID, teardown) — CC1101 transceiver, a fixed-code OOK encoder (PT2262/EV1527/HT12), or a rolling-code chip (HCS200/300/301 KeeLoq). The chip tells you the protocol family and which rtl_433 decoder or known attack applies.",
        "Device class and what the payload means — remote/fob, TPMS, weather/soil sensor, smart-home switch, wM-Bus meter, alarm contact. rtl_433's named decoders already cover hundreds of these, so identifying the class often hands you the field layout for free."
      ]
    },
    "SP": {
      "note": "Find where the device transmits and watch it fire. Sub-GHz lives in narrow ISM slices well below 1.7 GHz, so a cheap RTL-SDR is enough to see the whole thing — point it at 433.92 MHz (or 315/868/915) and look for the short bursts that appear each time you press the remote or the sensor reports. Two complementary views: a gqrx waterfall shows you the carrier, the modulation shape (OOK looks like blinking blocks, FSK like two stacked lines) and the rough timing; and pointing rtl_433 at the band turns every burst it recognises into a live JSON line naming the device — often the fastest way to confirm what is on the air and on which frequency, before you commit a capture tool. A HackRF works too and reaches the 868/915 bands with margin.",
      "tools": [
        { "tool": "gqrx", "role": "Spectrum / waterfall view", "why": "See the live sub-GHz spectrum and catch the bursts as you trigger the device — read the exact centre frequency, tell OOK (blinking blocks) from FSK (two parallel lines), and eyeball the burst timing before choosing a capture tool. Cheap and quick on an RTL-SDR since everything here sits below 1 GHz.", "deps": ["rtl-sdr-v4", "hackrf-one"] },
        { "tool": "rtl-433", "role": "Live device scan", "why": "Point rtl_433 at 433.92 MHz (or 315/868/915) and it decodes every burst it recognises into a JSON line naming the device and frequency — the fastest way to inventory what is transmitting around you and confirm a target's band and protocol without reading a waterfall by eye.", "caveat": "Only names devices it already has a decoder for; an unknown gadget still shows energy but no decode — fall back to the waterfall and URH.", "deps": ["rtl-sdr-v4", "hackrf-one"] },
        { "tool": "catnip", "role": "SX1262 spectrum analyzer", "why": "catnip drives the CatSniffer's Semtech SX1262 as a real-time sub-GHz spectrum analyzer — scan the 433/868/915 MHz bands and watch channel activity from the board, no SDR. A CatSniffer-native alternative to the gqrx waterfall for finding where a device transmits.", "deps": ["catsniffer"], "caveat": "A spectrum view to find activity; demodulating the burst is the SDR + rtl_433/URH or the CatSniffer (G)FSK path at the Capture step." }
      ]
    },
    "PHY": {
      "note": "No separate hardware demodulator step here — sub-GHz waveforms are simple (OOK/ASK or FSK at low baud), so the capture tools demodulate and frame in one pass and live at the next step. The PHY job is just a clean recording: sample the burst at a rate that comfortably covers its bandwidth, centred on the carrier you found at the Spectrum step, so the on/off edges (OOK) or the two tones (FSK) come through crisply for the demodulator to slice into bits."
    },
    "LL": {
      "note": "Capture the burst and turn it into bits and fields. For the OOK/ASK remotes that fill this band there is no PCAP-and-Wireshark flow — the radio tool is its own decoder. Three honest paths by how much the device is already understood: if it is a known device class, rtl_433 demodulates and decodes it straight to named JSON fields (ID, sensor values, checksum) — capture and decode in one command. If it is unknown, Universal Radio Hacker is the workbench: record the I/Q off an SDR, let it auto-detect the modulation and baud, extract the bitstream and label the fields by diffing several captures (e.g. press the button twice and see what changes). And for hands-on RX/TX from a pocket transceiver, rfcat drives a YARD Stick One to receive (and later replay) OOK/ASK/FSK from a Python shell, while the Flipper Zero captures a signal standalone — Read decodes a known protocol, Read RAW saves an unknown one for replay — with no laptop at all. The CatSniffer is the board exception: its Semtech SX1262 reads — and later transmits — (G)FSK packets on 433/868/915 MHz over a scriptable serial bridge, and its CC1352P firmware captures IEEE 802.15.4g (plus LoRa/Meshtastic) into Wireshark via catnip — covering the FSK/GFSK and framed device classes the SDR + rtl_433 OOK/ASK flow doesn’t.",
      "tools": [
        { "tool": "rtl-433", "role": "Primary decoder (known devices)", "why": "For any of its 320+ supported device protocols, rtl_433 demodulates the OOK/ASK or FSK burst and decodes it to named JSON fields in one step — the ID, the sensor/state values and the checksum — so a known TPMS, weather sensor or doorbell is captured and fully parsed at once. The default capture tool when the device is a recognised class.", "caveat": "Decodes only devices it has a protocol for; an unknown or proprietary remote needs URH instead.", "deps": ["rtl-sdr-v4", "hackrf-one"] },
        { "tool": "universal-radio-hacker", "role": "Reverse unknown signals", "why": "The workhorse for anything rtl_433 can't name: record the I/Q off an SDR, auto-detect modulation and bit rate, extract the bitstream, and diff several captures to label the fields (preamble, ID, payload, checksum). The fastest path from a raw OOK/FSK burst to a understood frame format — and it can resend what it captured.", "caveat": "Repository is archived (still the de-facto unknown-signal tool); a sample rate that covers the burst bandwidth matters.", "deps": ["hackrf-one", "rtl-sdr-v4", "yard-stick-one"] },
        { "tool": "rfcat", "role": "Pocket RX/TX (Python shell)", "why": "Drives a YARD Stick One as a CC1111 sub-GHz transceiver from a Python shell: set the frequency, modulation and baud you found, receive a burst and later replay or forge it — the reference cheap, scriptable sub-GHz work tool without an SDR.", "deps": ["yard-stick-one"] },
        { "tool": "flipperzero-firmware", "role": "Standalone field capture", "why": "The Flipper Zero's Sub-GHz app captures a signal standalone on its CC1101 radio — Read decodes a known protocol on the spot, Read RAW saves an unknown signal's raw timings — so you grab a remote in the field with no laptop or SDR and carry it straight to the replay step.", "caveat": "Official firmware will not save or replay rolling-code signals (a deliberate safety limit); fixed-code only on stock firmware.", "deps": ["flipper-zero"] },
        { "tool": "catnip", "role": "(G)FSK packet RX (scriptable)", "why": "The CatSniffer's Semtech SX1262 is a sub-1 GHz transceiver that does (G)FSK as well as LoRa, so catnip's SX1262 frame driver reads (G)FSK packets on 433/868/915 MHz over the serial bridge — scriptable in Python — and the CC1352P sniffer firmware additionally captures IEEE 802.15.4g (Wi-SUN / smart-meter mesh) and LoRa/Meshtastic into Wireshark. A board-based packet path for the many FSK/GFSK sub-GHz devices, no SDR.", "deps": ["catsniffer"], "caveat": "Reads framed (G)FSK packets — you set the frequency, modulation and bitrate; arbitrary OOK/ASK remotes still need the SDR + rtl_433/URH path. Captured packets can be transmitted back at the Attack step." }
      ]
    },
    "CR": {
      "note": "Be honest: for most sub-GHz devices there is nothing to 'crack', because there is no confidentiality to break. Fixed-code remotes and sensors transmit their payload in the clear — you read it at the Capture step, you do not decrypt it. The one thing that looks like crypto is the rolling code (KeeLoq / HCS301 and similar): each press sends a fresh value so an old capture won't replay, but that is replay resistance, not an encrypted channel you recover plaintext from. A rolling code is defeated by capturing and replaying a code the receiver hasn't seen yet (the Attack step), not by an offline break — and to truly forge the next valid code you would need the manufacturer's secret key programmed into the chip, which a passive capture never reveals. KeeLoq has known academic cryptanalysis (correlation/side-channel attacks to recover that manufacturer key), but those require physical or large-scale access far beyond a capture and are out of scope for this RF toolchain. So: read fixed codes, capture-and-replay rolling codes — there is no general offline cracker to point at sub-GHz."
    },
    "AT": {
      "note": "Going active means transmitting on the device's frequency to take over its function — do this ONLY on equipment you own or are explicitly authorised to test, and mind that even unlicensed ISM bands have power and duty-cycle rules. The attacks follow straight from the code type you identified. For a FIXED-code device the attack is trivial: replay the captured burst and the receiver acts on it (a gate opens, a sensor reading is spoofed) — re-send it with rfcat on a YARD Stick One, with URH's transmit, from a Flipper Zero, or generate it on a HackRF. For a ROLLING-code device a naive replay fails, so the classic technique is RollJam-style capture-and-block: jam the receiver while recording the victim's first press (so the device never acts on it and the code stays unused), let them press again and capture that too, forward the first code to operate the device now, and keep the second unused code to operate it later — a captured-but-unused valid code, not a broken cipher. Related techniques include de-syncing a rolling-code counter by replaying many codes to push it out of the receiver's window (denial of service), and brute-forcing a small fixed-code keyspace (e.g. 8–12 DIP-switch bits on old EV1527/PT2262 remotes). All of these need a transmit-capable radio.",
      "tools": [
        { "tool": "rfcat", "role": "Replay & transmit (fixed code)", "why": "From the Python shell, set the exact frequency/modulation/baud and re-transmit a captured fixed-code burst (or brute-force a short DIP-switch keyspace) through a YARD Stick One — the simplest replay against a fixed-code remote, gate or sensor.", "deps": ["yard-stick-one"], "needs": "The captured burst / decoded bits from the Capture step to re-send. Authorised testing only." },
        { "tool": "universal-radio-hacker", "role": "Replay & crafted-frame TX", "why": "URH transmits what it captured and lets you edit the bits first — flip a field, fix a checksum, fuzz a value — then send it on a TX-capable SDR or a YARD Stick One. The bridge from reversing an unknown frame to actively testing how the receiver responds to it.", "caveat": "Archived repo; transmits — authorised testing only.", "deps": ["hackrf-one", "yard-stick-one"], "needs": "A captured/edited frame from the Capture step and a transmit-capable radio." },
        { "tool": "flipperzero-firmware", "role": "Field replay (fixed code)", "why": "Replay a saved fixed-code signal from the Flipper Zero's Sub-GHz app in the field — open the gate or trigger the device you captured, no laptop needed.", "caveat": "Stock firmware refuses to save/replay rolling codes by design; fixed-code replay only without custom firmware.", "deps": ["flipper-zero"], "needs": "A signal saved at the Capture step. Authorised testing only." },
        { "tool": "catnip", "role": "(G)FSK transmit / replay (scriptable)", "why": "The CatSniffer's SX1262 transmits as well as receives, so it can re-send a captured (G)FSK packet — or a crafted one — on 433/868/915 MHz over the serial bridge, scriptable in Python: read a packet at the Capture step, then transmit it later to test replay or fuzzing against an FSK/GFSK device. A pocket transmit-capable sub-GHz board, no SDR.", "deps": ["catsniffer"], "needs": "A captured or crafted (G)FSK packet from the Capture step plus the target's frequency/modulation/bitrate. Transmits on ISM bands — authorised testing only, and mind power/duty-cycle limits.", "caveat": "(G)FSK packets, not arbitrary OOK/ASK signalling." }
      ]
    },
    "AP": {
      "note": "Above the radio burst, the 'application' of a sub-GHz device is just the meaning of the bytes it sends — a TPMS reporting pressure and temperature, a weather station sending humidity, a contact sensor saying open/closed, a meter sending consumption. There is no separate protocol stack to attack: rtl_433 already decodes hundreds of these device classes into named JSON fields, so reading the application layer is reading (and, at the Attack step, spoofing) those fields. Point rtl_433 at the device and its JSON output is the application view — the sensor values, IDs and flags the receiver trusts — which is exactly what you forge or replay to make a gateway or display believe a fake reading.",
      "tools": [
        { "tool": "rtl-433", "role": "Decoded device semantics (JSON)", "why": "rtl_433's JSON output is the application layer for sub-GHz sensors and remotes: each decoded burst becomes named fields — device ID, temperature/pressure/humidity, battery, open/closed, button — so you can read exactly what the receiver acts on, and know precisely which values to spoof or replay to drive it. The same decode that captured the device also exposes its semantics.", "deps": ["rtl-sdr-v4", "hackrf-one"], "needs": "The live device (or a recording) on a receive radio; rtl_433 prints the decoded fields as JSON for inspection or logging." }
      ]
    }
  }
};
