export default {
  "status": "complete",
  "facts": [
    { "k": "Band", "v": "2.4 GHz ISM — IEEE 802.15.4 O-QPSK PHY, the same radio layer as Zigbee" },
    { "k": "Channels", "v": "16 × 5 MHz, channels 11–26 (2.405–2.480 GHz); a Thread network sits on one channel" },
    { "k": "Stack", "v": "IPv6 mesh: 802.15.4 MAC → 6LoWPAN header compression → MLE (Mesh Link Establishment) routing → UDP. Matter (CHIP) rides on top." },
    { "k": "Thread security", "v": "MAC-layer AES-128-CCM* keyed by the Thread network key — strong link crypto. Commissioning: Commissioner authenticates with PSKc; a Joiner is admitted with PSKd over DTLS." },
    { "k": "Matter transport", "v": "Runs over Thread (via a Border Router) or Wi-Fi; commissioned over Bluetooth LE. Discoverable by DNS-SD: _matterc._udp = commissionable, _matter._tcp = operational, _meshcop._udp = a Thread Border Router." },
    { "k": "Matter onboarding", "v": "QR (MT: prefix, Base-38) / 11-digit manual code carry a 27-bit setup passcode + 12-bit discriminator (manual code only the upper 4-bit short discriminator), plus 16-bit Vendor ID and 16-bit Product ID." },
    { "k": "Matter crypto", "v": "PASE = SPAKE2+ (P-256) keyed from the setup passcode, only during the commissioning window; CASE = certificate-authenticated (NOC under a fabric Root CA, SIGMA over P-256) for all operational sessions. The passcode is the weak link, not the cipher." }
  ],
  "reference": null,
  "layers": {
    "IG": {
      "note": "Identify the mesh before any RF work — and first settle what you are even looking at. Thread and Zigbee share the exact same IEEE 802.15.4 2.4 GHz PHY, so a spectrum view alone cannot tell them apart; you distinguish them by what rides on top. Thread is an IPv6 mesh — its frames carry 6LoWPAN-compressed IPv6 and MLE link-management messages — whereas Zigbee carries its own NWK/APS layers. If the device is a Matter product, almost everything you need is in plain sight: a setup QR code / 11-digit numeric setup code on the label, a BLE radio used only for onboarding, and a transport you can read from DNS-SD — _matterc._udp means it is commissionable (waiting), _matter._tcp means it is already on a fabric, and a nearby _meshcop._udp means there is a Thread Border Router, so it is Matter-over-Thread rather than Matter-over-Wi-Fi. The onboarding payload itself carries the Vendor ID and Product ID, which the Distributed Compliance Ledger (DCL) resolves to a known vendor, product and certification record. The honest headline: Thread's link crypto and Matter's operational crypto are both strong, so the real attack surface is commissioning, weak or exposed setup codes, and the BLE onboarding window — not breaking AES.",
      "lookFor": [
        "Is it Thread or Zigbee? Both are 802.15.4 at 2.4 GHz — tell them apart by the upper layers: 6LoWPAN + MLE (and IPv6) means Thread; a Zigbee NWK/APS stack means Zigbee.",
        "Is it a Matter device, and over which transport? Look for the Matter logo, a setup QR code and an 11-digit numeric setup code on the label, and a BLE radio for onboarding. Read the transport from DNS-SD: _matterc._udp = commissionable, _matter._tcp = operational; a co-located _meshcop._udp Border Router means Matter-over-Thread, otherwise it is on Wi-Fi.",
        "Decode the onboarding payload (QR 'MT:' Base-38, or the 11-digit manual code): it carries a 27-bit setup passcode, the device discriminator (12-bit, or the upper 4-bit short discriminator in the manual code), the discovery/rendezvous capabilities, and — in the QR / 21-digit manual code — the 16-bit Vendor ID and 16-bit Product ID.",
        "Resolve the Vendor ID / Product ID against the Distributed Compliance Ledger (DCL) — vendor and product identity, certification status, and the PAA attestation roots. A Test-Vendor VID (0xFFF1–0xFFF4) on a shipping product is a red flag.",
        "Chipset / SoC and SDK (FCC ID, teardown) — e.g. nRF52840, EFR32, ESP32-H2/C6 — and cross-reference known Thread/Matter/802.15.4 stack CVEs (e.g. CASE Sigma1-replay DoS CVE-2024-3297, fabric-footprinting CVE-2024-3454).",
        "Network identifiers: the 802.15.4 channel (11–26), PAN ID and Extended PAN ID, and the Network Name — all readable from beacons/MLE without any key.",
        "Commissioning posture: how is the network key distributed, is a Border Router / Border Agent reachable, and is the commissioning credential (Thread PSKc / Joiner PSKd, or the Matter setup passcode) exposed, default, or weak? Is the BLE commissioning window left open, and can you read the setup passcode off the printed label?"
      ],
      "tools": [
        { "tool": "matter-dcl", "role": "Resolve VID/PID & trust roots", "why": "Look up the Vendor ID / Product ID decoded from the onboarding payload against the CSA Distributed Compliance Ledger: who made the device, which product it is, its certification status, and the PAA attestation roots a commissioner trusts — turning the numbers on the QR code into a known device and flagging Test-Vendor VIDs (0xFFF1–0xFFF4).", "deps": [], "needs": "The Vendor ID / Product ID read from the QR or 21-digit manual onboarding payload; query the public DCL web UI or a DCL node. No radio." },
        { "tool": "chip-tool", "role": "Decode payload & discover", "why": "Parse a scanned QR / manual setup code to recover the passcode, discriminator and VID/PID, and discover commissionable Matter nodes over BLE and on-network (DNS-SD) to confirm the transport and that an onboarding window is open — before any commissioning attempt.", "deps": ["usb-bt-dongle", "catsniffer"], "needs": "A host Bluetooth LE adapter for BLE discovery of commissionable devices; for Matter-over-Wi-Fi, on-network DNS-SD discovery needs no BLE radio." }
      ]
    },
    "SP": {
      "note": "How much of the band can you see, and which channel is the mesh on? IEEE 802.15.4 divides the 2.4 GHz band into 16 channels (11–26) of 5 MHz each across roughly 2.405–2.480 GHz, and a Thread network lives on just one of them. The first job is to find that channel. A wide SDR view shows the whole 80 MHz at a glance so you can spot the active channel; you then point a real 802.15.4 radio at it to capture. A HackRF shows ~20 MHz at a time (a few channels); a bladeRF in its oversampling mode covers the full band in one pass. RTL-SDR is out — it stops below the 2.4 GHz band. (Matter's BLE onboarding lives on the BLE advertising channels, not here — that is a Bluetooth LE capture; see the BLE wayfinder.)",
      "tools": [
        { "tool": "gqrx", "role": "Spectrum / waterfall view", "why": "See the live 2.4 GHz spectrum and pick the active 802.15.4 channel — ~20 MHz at a time on a HackRF, the full band on a bladeRF (oversampling) — before committing a sniffer to it.", "deps": ["hackrf-one", "bladerf-2-micro"] },
        { "tool": "minino", "role": "Pocket 802.15.4 scanner", "why": "Electronic Cats ESP32-C6 multitool with an 802.15.4 (Zigbee/Thread) field-recon mode: scan channels and spot an active mesh standalone, no laptop or SDR needed.", "caveat": "Recon-level visibility; confirm Thread vs Zigbee from a real frame capture.", "deps": [] },
        { "tool": "catnip", "role": "Channel activity & topology (cativity)", "why": "catnip's cativity monitor shows live 802.15.4 channel activity (11–26) and discovers the network topology of a Thread / Zigbee mesh — a quick CatSniffer-native way to find the active channel and map the mesh before committing a capture radio.", "deps": ["catsniffer"] }
      ]
    },
    "PHY": {
      "note": "No standalone Thread demodulator in this kit. The capture tools demodulate the 802.15.4 O-QPSK waveform (PHY) and frame the packets (LL) together on a dedicated 2.4 GHz 802.15.4 radio, so they live at the Capture step. A wide SDR is useful for finding the channel, but a real 802.15.4 transceiver does the actual framing."
    },
    "LL": {
      "note": "Capture and decode 802.15.4 / Thread frames. Park a real 802.15.4 radio on the target channel and it pulls the raw frames — beacons, MAC, 6LoWPAN, MLE — into Wireshark, which has a full 802.15.4 + Thread/6LoWPAN/MLE dissector. The crucial trick: Thread's MAC payload is AES-128-CCM* encrypted under the network key, but Wireshark will decrypt it in place once you give it that key (the decrypt step lives in 'Break the crypto'). Pick a capture path: the turnkey Nordic extcap, an OpenThread co-processor via pyspinel, or the standalone Minino. Each runs on its radio and feeds Wireshark. (Matter's BLE commissioning handshake is captured separately as Bluetooth LE traffic — see the BLE wayfinder.)",
      "decoder": "wireshark",
      "tools": [
        { "tool": "nrf-sniffer-802154", "role": "Primary sniffer", "why": "Nordic's nRF Sniffer for 802.15.4: firmware plus a Wireshark extcap plugin that streams raw 802.15.4/Thread frames straight into Wireshark — the simplest, best-documented capture path. Runs on an nRF52840 dongle.", "deps": ["nrf52840-dongle"] },
        { "tool": "pyspinel", "role": "OpenThread sniffer", "why": "Its sniffer.py turns an OpenThread NCP/RCP into an 802.15.4 sniffer, piping frames to Wireshark — and the same device can also scan for and attempt to join the mesh, so capture and commissioning assessment share one radio.", "deps": ["nrf52840-dongle"], "needs": "An nRF52840 (or other supported radio) flashed with OpenThread as an NCP/RCP; pyspinel drives it over Spinel." },
        { "tool": "catsniffer", "role": "Multiprotocol sniffer", "why": "CC1352-based multiprotocol radio that captures 802.15.4 alongside its BLE/Sub-GHz/Zigbee workflows and exports PCAP for Wireshark — one board for the whole 2.4 GHz mesh kit, and the same board can capture the BLE onboarding traffic.", "deps": [] },
        { "tool": "minino", "role": "Pocket sniffer", "why": "Standalone ESP32-C6 802.15.4 (Zigbee/Thread) capture to microSD with Wireshark-compatible output — capture in the field with no host PC.", "caveat": "2.4 GHz field recon; less control than the host-driven sniffers.", "deps": [] },
        { "tool": "whad", "role": "802.15.4 sniff (multi-protocol)", "why": "WHAD captures raw 802.15.4 — the layer Thread rides on — from an nRF52840 (its Butterfly firmware) or an APIMote into PCAP/Scapy via one Python framework, the same one you use for Zigbee and BLE. Feed the PCAP to Wireshark's Thread/6LoWPAN/MLE dissector.", "deps": ["nrf52840-dongle", "apimote"] }
      ]
    },
    "CR": {
      "note": "Be honest about the crypto: both layers here are strong. Thread's MAC is AES-128-CCM* and Matter's sessions are SPAKE2+ (commissioning) then certificate-authenticated CASE (operation) — there is no offline key-recovery attack the way crackle attacks weak BLE pairing. So this step is decrypt and credential-recovery, not cipher-breaking. On the Thread side: if you hold the Thread network key, Wireshark decrypts the whole capture in place and the mesh becomes readable — the question is whether you can obtain that key from weak/default/exposed commissioning credentials (PSKc / Joiner PSKd). On the Matter side: PASE keys the very first session from the 27-bit setup passcode via SPAKE2+, which is only as strong as that passcode — a default, guessable, or printed-and-photographed passcode collapses the search. The device stores a SPAKE2+ verifier, not the passcode; a verifier extracted from an insecure device is open to offline recovery given the low passcode entropy. Online guessing is rate-limited — a node exits commissioning mode after ~20 failed PASE attempts and the window is time-boxed (≤15 min once on a fabric) — and the spec disallows the obvious codes (00000000, 11111111…, 12345678, 87654321), so the real win is a weak vendor default or an exposed onboarding payload, not brute force against a random code. No radio of its own; it consumes a capture or a recovered verifier.",
      "tools": [
        { "tool": "wireshark", "role": "Decrypt Thread with the network key", "why": "Wireshark's 802.15.4 layer decrypts CCM*-protected Thread frames in place once you supply the Thread network key in its 802.15.4 decryption-keys table — turning an opaque capture into readable 6LoWPAN/MLE/IPv6.", "deps": [], "needs": "A capture of the mesh from the Capture step (nRF Sniffer / pyspinel / CatSniffer / Minino), plus the Thread network key. The key is the prize — it comes from weak/default/exposed commissioning credentials, not from breaking AES." },
        { "tool": "chip-tool", "role": "Matter PASE / passcode test", "why": "Drive PASE (SPAKE2+) against the device with a candidate setup passcode — the default/printed value, or a small guess set — to test whether a weak or exposed passcode lets you establish the first session. Where it succeeds, you can go on to commission the node; where it fails ~20 times the device leaves commissioning mode, which is itself a finding about the rate-limit posture.", "deps": ["usb-bt-dongle", "catsniffer"], "needs": "A host Bluetooth LE adapter (BLE onboarding transport), an open commissioning window, and the candidate setup passcode/discriminator — from the printed QR/label, a known vendor default, or an allowed-range guess. SPAKE2+ has no offline shortcut against a strong random passcode; this targets weak or exposed codes only." }
      ]
    },
    "AT": {
      "note": "The real attack surface is commissioning and fabric onboarding, not the link cipher. Taking over a Thread/Matter device means joining or subverting the mesh or its fabric, not hijacking an established 802.15.4 frame stream. Pick the angle: stand up an OpenThread node and try to join the Thread network using captured/guessed commissioning credentials (then you are inside the mesh and the network key is yours); or attack Matter onboarding directly — run your own commissioner against a device whose BLE commissioning window is open or whose setup passcode is weak/exposed, complete PASE and commission the node onto a fabric you control. Matter is explicitly multi-admin: a device can sit on several fabrics at once, and an existing admin can open a fresh commissioning window (Open Commissioning Window, with a new passcode and verifier) to add a second admin — so the abuse questions are whether that window can be opened or hijacked, whether a freshly issued passcode can be intercepted out-of-band, and whether you can stand up a rogue commissioner during the window. Note the controller is not required to prove trustworthiness to the device, so anyone who clears commissioning becomes a full admin. Where the crypto holds, this credential-and-onboarding path is the way in.",
      "tools": [
        { "tool": "pyspinel", "role": "Join / probe the mesh", "why": "Drive an OpenThread node to scan, beacon-request and attempt to commission/join the target Thread network using the credentials you obtained — if it joins, you are inside the mesh and hold the network key.", "deps": ["nrf52840-dongle"], "needs": "An nRF52840 flashed with OpenThread (NCP/RCP), plus commissioning credentials (Joiner PSKd / network key) recovered at Identify or Break-the-crypto." },
        { "tool": "chip-tool", "role": "Commission onto your fabric", "why": "Run your own Matter commissioner against the device over BLE: if the commissioning window is open or the setup passcode is weak/exposed, complete PASE and commission the node onto a fabric you control (`pairing ble-thread` / `pairing ble-wifi`, or `pairing code` on-network) — then test multi-admin by opening a second window with `pairing open-commissioning-window`.", "deps": ["usb-bt-dongle", "catsniffer"], "needs": "A host Bluetooth LE adapter for the BLE onboarding transport, and the device's setup passcode/discriminator (from the QR code, label, or a guessable/exposed value). For Matter-over-Thread you also supply the Thread operational dataset; for Matter-over-Wi-Fi, the Wi-Fi SSID/credentials." },
        { "tool": "chip-repl", "role": "Scripted multi-fabric abuse", "why": "Same CHIP controller stack as chip-tool but in Python — script repeated PASE attempts, automate commissioning onto an attacker fabric, and drive multi-admin / multi-fabric tests (open a window, add a second admin, enumerate fabrics) programmatically rather than by hand.", "deps": ["usb-bt-dongle", "catsniffer"], "needs": "A host Bluetooth LE adapter for BLE onboarding and the device's setup passcode/discriminator; the connectedhomeip Python controller (matter-repl.py)." }
      ]
    },
    "AP": {
      "note": "Once you are commissioned onto the device (or onto the mesh), test what it exposes above the link. For Matter that is the cluster/attribute model — read state, invoke commands, subscribe to reports — all over CASE-secured operational sessions. The questions here are what the device trusts a commissioned controller to do, whether its Access Control List (ACL) actually constrains a freshly added admin, and whether the application layer enforces the access controls Matter's spec expects. Because commissioning makes you a full fabric admin, the application layer is often wide open to a controller that got in — which is exactly why the commissioning gate matters so much.",
      "tools": [
        { "tool": "chip-tool", "role": "Matter cluster interaction", "why": "After commissioning, use CASE-secured operational commands to read/write attributes, invoke cluster commands and subscribe to a Matter device — the way to enumerate and exercise its application layer and probe its Access Control List.", "deps": ["usb-bt-dongle", "catsniffer"], "needs": "A commissioned Matter node (from the Attack step) and a controller fabric; chip-tool from the connectedhomeip SDK speaks the operational Matter protocol over Thread or Wi-Fi (the BLE adapter is only needed for the initial commissioning)." },
        { "tool": "chip-repl", "role": "Scripted cluster enumeration", "why": "Walk the whole cluster/attribute tree, batch reads and subscriptions, and replay learned commands from Python — better than chip-tool for systematically enumerating a device's exposed application surface and access controls.", "deps": ["usb-bt-dongle", "catsniffer"], "needs": "A commissioned Matter node and the connectedhomeip Python controller; operational traffic rides Thread or Wi-Fi, so no BLE radio is needed post-commissioning." },
        { "tool": "python-matter-server", "role": "Standing controller / API", "why": "Run a persistent CSA-certified Matter controller (the one behind Home Assistant) and drive the commissioned node over its WebSocket API — convenient when you want a long-lived controller to enumerate clusters, watch subscriptions over time, and integrate the device into a test harness.", "deps": ["usb-bt-dongle", "catsniffer"], "needs": "A commissioned Matter node and a host with the CHIP stack; a BLE adapter only if you also use it to perform the initial commissioning. Operational interaction is over Thread/Wi-Fi." }
      ]
    }
  }
};
